export edu.only.ksp.application.queries._share.model.JRole
    -> package edu.only.ksp.application.queries._share.draft.JRole

RolePermissionInfo {
    rolePermissions {
        permissionCode

        permissionRemark: String
        assigned: Boolean = true
    }
}
